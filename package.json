{
  "name": "cypress",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "confAlice": "npx cypress open --config-file cypress.alice.js",
    "confYears": "npx cypress open --config-file cypress.years.js",
    "headless": "npx cypress run --headless --browser chrome",
    "edge": "npx cypress run --headed --browser edge"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cypress": "^13.15.0",
    "cypress-file-upload": "^5.0.8"
  },
  "devDependencies": {}
}
